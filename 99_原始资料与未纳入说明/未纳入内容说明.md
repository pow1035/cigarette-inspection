# 未纳入整理版的内容

## 已单独归档的大文件

以下内容有价值，但不适合混在源码目录里，所以已经单独放到：

`07_运行环境与依赖包`

- `qt-opensource-windows-x86-5.9.9.exe`
  - Qt 5.9.9 Windows 安装包，约 2.4G。
  - 后续需要在 Windows 现场机搭 Qt 编译环境时有用。

- `opencv-4.8.0-windows.exe`
  - OpenCV 安装包。
  - 后续需要配置 OpenCV 或重建 TensorRT 推理 demo 时有用。

- `AI推理测试运行包_来自build_trt_Release`
  - 从 `expert_test/build_trt/Release` 中提取。
  - 保留了非重复的 `.dll`、测试 `.exe`、ONNX、TensorRT engine、测试图片、`run_test.bat` 和 `README.txt`。
  - 用途是复现原作者的 YOLO/TensorRT Windows 测试环境。

## 仍未纳入的大文件

以下内容仍然没有放入整理版，因为基本没有长期价值：

- 各类 `.obj`、`.pdb`、`.ilk`、`.idb`、`.tlog`、`.recipe`、`.lastbuildstate`
  - Visual Studio 编译中间文件。
  - 可以重新编译生成。

- `.vs`、`x64/Debug`、`x64/Release`
  - IDE 缓存和构建输出。
  - 不适合作为源码整理内容。

- `output_test*.jpg`
  - 运行 demo 后生成的输出图。
  - 可以随时重新生成。

## 去掉的重复文件

原始资料中有大量带 `(1)` 的重复文件，例如：

- `xxx(1).cpp`
- `xxx(1).dll`
- `xxx(1).jpg`
- `xxx(1).pt`

整理版默认只保留不带 `(1)` 的版本。

## 原始资料位置

为了避免误删烟厂给出的原始证据链，我没有删除原始压缩包。当前原始 zip 仍在：

`/Users/c/Library/Containers/com.tencent.WeWorkMac/Data/Documents/Profiles/BF6DF626BB54BCB7E8856A6A3AD9ACBC/Caches/Files/2026-07/E7B64CEA9AD785D5EF67ED5239BDA73B/崔鑫毅.zip`

工作区中的原始解压内容仍在：

`/Users/c/Documents/Codex/2026-07-09/ni-x/work/project_read/崔鑫毅`

如果后续确认整理版没有遗漏，再考虑清掉工作区原始解压目录以节省空间。
