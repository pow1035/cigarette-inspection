# 架构决定记录

| ID | 日期 | 决定 | 原因 | 状态 |
| --- | --- | --- | --- | --- |
| D-001 | 2026-07-10 | `01_上位机_QT_新版_CigVision/源码` 是最终产品主线 | 新版已有 UI/参数/相机/IO 骨架，适合作为后续集成宿主 | 已采用 |
| D-002 | 2026-07-10 | 老版 `testQT` 只作为时序和业务规则参考 | 老版闭环更完整，但包含硬编码、全局状态和生命周期风险 | 已采用 |
| D-003 | 2026-07-10 | 深度学习是主要检测路线 | 符合项目当前目标，现有 TensorRT 原型可作为起点 | 已采用 |
| D-004 | 2026-07-10 | 先建立 `IDetector` 边界，再接 TensorRT | 降低 UI/运行层与模型实现耦合，便于离线测试和可选传统兜底 | 已采用 |
| D-005 | 2026-07-10 | 大依赖包保留本机但不进入普通 Git | `07_运行环境与依赖包` 体积约 6.4G，仓库主要同步源码和可控资产 | 已采用 |
| D-006 | 2026-07-10 | 真实剔除输出默认关闭 | 误写 IO 可能造成设备动作和生产风险 | 已采用 |
| D-007 | 2026-07-10 | 按小阶段、证据矩阵和独立评审推进 | 项目跨 Qt、视觉、GPU、相机、IO，长任务必须可恢复和可审计 | 已采用 |
| D-008 | 2026-07-10 | `docs/` 使用稳定英文文件名、中文内容 | 兼容 skill 的固定入口，也方便项目成员阅读 | 已采用 |

| D-009 | 2026-07-10 | 相机在初始化阶段只打开和配置，由 Run/Stop 显式开始或停止取流 | 防止窗口启动即采集，并使运行按钮具有真实状态语义 | 已采用 |
| D-010 | 2026-07-10 | P1 排除陈旧 testWrite 编译项，真实剔除保持关闭 | testWrite 引用老版 testQT 且无有效实现，不能作为新版输出链路 | 已采用 |
| D-011 | 2026-07-10 | 暂不统一 Debug/Release 的 Halcon 版本 | 目标 Windows 安装情况未知，先记录分裂并用真实构建证据决定 | 临时决定 |
| D-012 | 2026-07-11 | P2 核心契约和接口只依赖 C++14 标准库 | 允许脱离 Qt、Halcon、MVS、DAQNavi 和 TensorRT 构造、替换和测试 | 已采用 |
| D-013 | 2026-07-11 | RejectCommand 表达调度意图，RejectExecutionResult 单独表达执行回执 | 防止输出适配器反向修改命令，并为模拟、执行、跳过和失败提供明确结果 | 已采用 |
| D-014 | 2026-07-11 | P2 不立即替换 picStruct 或相机队列 | 当前没有消费者；直接迁移会越过 P2 并把回调、Halcon、IO 编号和线程模型一起改动 | 已采用 |
| D-015 | 2026-07-11 | P3 使用独立离线会话和 Qt 适配器，不复用在线相机线程池 | 隔离图片回放与硬件生命周期，允许无相机/无 IO 的可重复测试和停止 | 已采用 |
| D-016 | 2026-07-11 | P3 fixture detector 只验证编排，名称、UI 和证据均标注非生产算法 | 当前无可信 ground truth/模型评估，不能用确定性假结果冒充检测准确率 | 已采用 |
| D-017 | 2026-07-11 | 离线结果 JSON 与 PNG 使用 `QSaveFile` 原子提交 | 保存失败必须可观察，并避免把半写文件当成完整结果 | 已采用 |
| D-018 | 2026-07-11 | P4 使用 TensorRT 10 名称 API 和独立 `IDetector` 适配器，不复用 TensorRT 8.6 demo | 旧 engine 无法在 10.15 反序列化，旧 binding API、错误处理和输出假设不满足产品门禁 | 已采用 |
| D-019 | 2026-07-11 | 固定预处理采用 OpenCV 4.9 `INTER_LINEAR` stretch RGB CHW | 自写双线性在 NMS 临界框上放大为输出差异；OpenCV 路径与 ONNX Runtime/TensorRT 对照一致 | 已采用 |
| D-020 | 2026-07-11 | 当前候选采用 FP16 engine，同时保留 FP32 对照；无人工真值时只做输出一致性、视觉审查和时延 | 12 图 FP16/FP32 检测数与类别顺序一致、最大置信度差约 0.02；精度指标没有可复现标签支撑 | 临时决定 |
| D-021 | 2026-07-11 | 当前 P5-P8 全部改为本地算法效果、实时仿真、Qt 产品化和交付预验收；现场硬件阶段冻结 | 用户当前无法到现场，商业项目应先把可在本机证明的准确性、速度、体验和稳定性做好，同时避免把仿真冒充现场证据 | 已采用 |

后续改变产品主线、真实 IO 安全策略、模型输入方式或传统算法兜底范围时，必须新增决定记录，不覆盖旧记录。
